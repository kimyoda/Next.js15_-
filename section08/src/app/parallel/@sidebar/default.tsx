/**
 * @sidebar 슬롯의 기본 컴포넌트
 *
 * 이 컴포넌트는 @sidebar 슬롯에 대한 기본 뷰를 제공합니다.
 * 다른 페이지에서 @sidebar 슬롯을 오버라이드하지 않을 때
 * 이 컴포넌트가 표시됩니다.
 */
export default function Default() {
  return <div>@sidebar/default</div>;
}
